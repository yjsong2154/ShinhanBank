import AvatarSOL from "../components/AvatarSOL/AvatarSOL";

export default function AniamtionPage() {
    return (
        <div style={{ padding: 24 }}>
            <AvatarSOL size={300} character={1} cloth={2} hat={3}/>
        </div>
    );
}